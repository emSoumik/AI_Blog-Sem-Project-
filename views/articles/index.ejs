<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>Blog</title>
    <style>
        .thick-dark-line {
            border: 0;
            height: 5px; /* Adjust the thickness as needed */
            background-color: #000; /* Dark color */
            margin-bottom: 20px; /* Space below the line */
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">
                <img src="/images/logo-white.png" alt="logo" class="img-fluid" style="max-height: 100px;"> <!-- Adjust the max-height as needed -->
            </a>
            <a href="/login" class="btn btn-primary">Login</a>
        </div>
    </nav>
    <hr class="thick-dark-line">
    <div class="container">
        <a href="/articles/new" class="btn btn-success mb-4">New Article</a>
        
        <% articles.forEach(article => { %>
            <div class="card mt-4">
                <div class="card-body">
                    <h4 class="card-title"><%= article.title %></h4>
                    <div class="card-subtitle text-muted mb-2">
                        <%= article.createdAt.toLocaleDateString() %>
                    </div>
                    <div class="card-text mb-2">
                        <%= article.description %>
                    </div>
                    <a href="articles/<%= article.id %>" class="btn btn-primary">Read more</a>
                </div>
            </div>
        <% }) %>
    </div>
</body>
</html>